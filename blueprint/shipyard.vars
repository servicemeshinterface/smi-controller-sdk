# Name of the network to attach the Kubernetes cluster running Consul to
consul_network = "dc1"

# Name of the cluster to deploy charts to
consul_k8s_cluster = "dc1"

# Name of the kubernetes cluster
monitoring_k8s_cluster = "dc1"

# Name of the network for attaching monitoring ingress resources too
monitoring_network = "dc1"
